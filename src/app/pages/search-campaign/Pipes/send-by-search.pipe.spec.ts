import { SendBySearchPipe } from './send-by-search.pipe';

describe('SendBySearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SendBySearchPipe();
    expect(pipe).toBeTruthy();
  });
});
